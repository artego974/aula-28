

function calcularRaizQuadrada(numero) {
    return Math.sqrt(numero);
}


let numero = 16;
let raizQuadrada = calcularRaizQuadrada(numero);
console.log("A raiz quadrada de", numero, "é:", raizQuadrada); 